import styled from 'styled-components'
import config from '../../../styles/config';

const StyledCountry = styled.div`
  display: block;
  color: ${config.color.graydark};
  font-size: 0.8rem;
  `;
  
export default StyledCountry